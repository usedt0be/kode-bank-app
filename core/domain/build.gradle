plugins {
  id "android-library-convention"
  id "dagger-convention"
  id "android-compose-convention"
  alias(libs.plugins.sqldelight)
}

android {
  namespace 'ru.kode.base.internship.core.domain'
}

dependencies {
  api project(':core')
}
